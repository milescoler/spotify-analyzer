# Dependencies (the app ships with none, but keep this for tooling)
node_modules/

# Runtime data — regenerated/seeded on first boot
data/data.json

# Secrets — never commit a real key (.env.example is the template)
.env

# Logs & OS files
*.log
.DS_Store

# Editor
.vscode/
.idea/
*.swp
*.swo
